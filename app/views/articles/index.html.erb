<h1 class="text-center mt-4">This is the ARTICLES LISTING page</h1>

<div class="flickr_pagination mt-4 mb-4">
    <%= will_paginate @articles, :container => false %>
</div>

<%= render 'article' %>

<div class="flickr_pagination mt-4 mb-4">
    <%= will_paginate @articles, :container => false %>
</div>

<!-- Table view -->
<!--
<div class="container">
    <table align="center" class= "table table-striped table-hover">
        <thead align="center">
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody align="center">
            <% @articles.each do |art| %>
            <tr>
                <td><%= art.title %></td>
                <td><%= art.description %></td>
                <td>
                    <button><%= link_to 'Show', article_path(art) %></button>
                    <button><%= link_to 'Edit', edit_article_path(art) %></button>
                    <button><%= link_to 'Delete', article_path(art), method: :delete, data: {confirm: "Are you sure?"} %></button>
                </td>
            </tr>
            <% end %>
        </tbody>
    </table>
</div> %>
-->

<p class="mt-4 text-center">
    <%= link_to 'New Article', new_article_path, class: "btn btn-outline-success btn-lg" %>
</p>